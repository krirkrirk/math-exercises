import { exercises } from './exercises/exercises';

const allExercises = [...exercises];

// exos.forEach((exo) => {
//   console.log(exo);
//   console.log(exo.generator(10));
// });
export { allExercises };
